{
  "name": "transover",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:content": "vite build --config vite.content.config.ts",
    "build:popup": "vite build --config vite.popup.config.ts",
    "copy:static": "cp src/manifest.json dist/manifest.json && cp -r src/icons dist/icons",
    "build": "npm run build:content && npm run build:popup && npm run copy:static",
    "dev": "npm run build:content -- --watch & npm run build:popup -- --watch",
    "preview": "echo 'Load the dist/ folder as an unpacked extension in chrome://extensions'"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/chrome": "^0.0.295",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
